{{> header }}

<div class="container">

	<div class="jumbotron">
		<h1>{{{site-title}}}</h1>
		<p>{{{intro}}}</p>
		<button class="btn icon-ok"> button text</button>
	</div>

	<div class="row-fluid">
		<section class="col-span-8">
			<h2>Heading 2</h2>
			<p>{{{section_dummy}}}</p>
		</section>
		<aside class="col-span-4">
			<h2>Heading 2</h2>
			<p>{{{aside_dummy}}}</p>
		</aside>
	</div>

</div>

{{> footer }}

